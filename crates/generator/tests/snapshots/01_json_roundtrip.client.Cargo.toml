# Generated by openapi-to-rust generator 0.1 (main spec §3/§3.1). DO NOT EDIT.
#
# Embedded toolchain tuple (§3.1): axum 0.8, reqwest 0.12, http 1, bytes 1
# MSRV 1.85. Requirements are caret-implied and pinned to this
# generator release; never path or pre-release dependencies.

[package]
name = "widgets-api"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"

[features]
default = ["client"]
client = ["openapi-support/client", "dep:reqwest", "dep:hyper", "dep:tokio", "dep:tokio-util", "dep:futures-util"]
server = ["openapi-support/server", "dep:axum", "dep:tokio", "dep:tokio-util", "dep:futures-util"]

[dependencies]
async-trait = "0.1"
axum = { version = "0.8", features = ["multipart"], optional = true }
bytes = "1"
futures-core = "0.3"
futures-util = { version = "0.3", optional = true }
http = "1"
hyper = { version = "1", optional = true }
mime = "0.3"
openapi-support = "0.1"
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "stream"], optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["fs", "io-util"], optional = true }
tokio-util = { version = "0.7", features = ["io"], optional = true }
